// @ts-check
import { defineConfig } from 'astro/config';

// Kept deliberately small. Every extra option here is one more thing
// that has to be explained later. Add only what earns its place.
export default defineConfig({
  site: 'https://marios-konomis.pages.dev',
});
